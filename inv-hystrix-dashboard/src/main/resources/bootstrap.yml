---
spring:
  application:
    name: inv-hystrix-dashboard
  cloud:
    config:
      discovery.enabled: true
      discovery.serviceId: config-server  

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/          

server:
  port: ${port:8182}
      
      
